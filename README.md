# tds-project-2

# TDS Quiz Solver Endpoint

This is my submission for the TDS Project Quiz Solver.  
It exposes a single POST endpoint `/` which takes:

```json
{
  "email": "...",
  "secret": "...",
  "url": "https://..."
}
